실시간 이메일체크기능, 회원가입 페이지 호출
---
tags:
  - 회원가입
parameters:
  - in: body
    name: mode
    type: string
  - in: body
    name: Email
    type: string
responses:
  200:
    description: joinpage + msg result
